{
  "name": "matchpulse-ai-stadium-digital-twin",
  "version": "2.6.0",
  "description": "MatchPulse AI — 3D Digital Twin for FIFA World Cup 2026 (Dual Portal: Fan & Committee Control Room)",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "NODE_ENV=production node server.js",
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "dev": "concurrently \"node server.js\" \"vite --host 0.0.0.0 --port 3000\"",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3000"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "concurrently": "^10.0.3",
    "vite": "^5.3.1",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@langchain/core": "^1.2.2",
    "@langchain/groq": "^1.3.1",
    "@langchain/langgraph": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "three": "^0.128.0"
  }
}
